---
interface Props { src: string; alt: string }
const { src, alt } = Astro.props;
---
<figure class="plot">
  <a href={src}><img src={src} alt={alt} loading="lazy" /></a>
  <figcaption><slot /></figcaption>
</figure>
