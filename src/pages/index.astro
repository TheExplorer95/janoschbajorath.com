---
import Base from '../layouts/Base.astro';
import EntryList from '../components/EntryList.astro';
import { getCollection } from 'astro:content';
import { site } from '../data/site';

const research = (await getCollection('research')).sort((a, b) => b.data.date.valueOf() - a.data.date.valueOf()).slice(0, 3);
---
<Base>
  <section class="hero">
    <img class="portrait" src="/portrait.jpg" alt="" width="160" height="160" />
    <div class="text">
      <h1>{site.name}</h1>
      <p class="lede">{site.tagline}</p>
      <p>
        I study how quadruped robots can learn to walk, trot and bound from raw proprioception —
        and what the representations they build along the way can tell us about the robot, its motion, and the training itself.
      </p>
      <div class="button-row">
        <a href={`mailto:${site.email}`}>Email</a>
        {site.links.map((l) => <a href={l.href}>{l.label}</a>)}
      </div>
    </div>
  </section>

  <h2>Recent</h2>
  <EntryList base="/research" entries={research} />
  <p style="margin-top:1rem"><a href="/research">All research →</a> · <a href="/teaching">Teaching →</a></p>
</Base>
